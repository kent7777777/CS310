#include <stdio.h>
#include <stdlib.h>
int main(int argc, int[] argv){
	char c;
	double sum = 0;
	for(int i = 0; i < strlen(argv[1]); i++){
		if(argv[1][i] == 'i'){
			sum = sum + atod(argv(i + 2));
		}else if(argv[1][i] == 'i'){
			sum = sum +
